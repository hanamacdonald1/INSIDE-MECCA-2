import { buildMetadata } from "../seo";
import { SeoLayout } from "../seo-layout";
export const metadata = buildMetadata({ title: "Project Updates | Inside MECCA", description: "Track completed work, current investigation activity, evidence gaps, corrections and next milestones for the Inside MECCA workplace investigation.", path: "/updates" });
export default SeoLayout;

import { buildMetadata } from "../seo";
import { SeoLayout } from "../seo-layout";

export const metadata = buildMetadata({
  title: "Private Audience Insights | Inside MECCA",
  description: "Restricted, privacy-safe audience insight and action workspace.",
  path: "/audience-insights",
  noIndex: true,
});

export default SeoLayout;


import { buildMetadata } from "../seo";
import { SeoLayout } from "../seo-layout";
export const metadata = buildMetadata({ title: "MECCA Workplace Investigation Methodology", description: "How Inside MECCA collects, codes, corroborates and reviews workplace evidence, including contradictions, consent, right of reply and publication thresholds.", path: "/methodology" });
export default SeoLayout;

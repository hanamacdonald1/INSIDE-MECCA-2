import { buildMetadata } from "../seo";
import { SeoLayout } from "../seo-layout";
export const metadata = buildMetadata({ title: "Private Submissions | Inside MECCA", description: "Restricted research administration area.", path: "/submissions", noIndex: true });
export default SeoLayout;

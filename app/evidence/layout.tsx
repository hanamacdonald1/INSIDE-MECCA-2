import { buildMetadata } from "../seo";
import { SeoLayout } from "../seo-layout";
export const metadata = buildMetadata({ title: "Evidence Standards | Inside MECCA", description: "See how Inside MECCA distinguishes testimony, records, allegations, corroboration, analysis and findings in its workplace investigation.", path: "/evidence" });
export default SeoLayout;

import { buildMetadata } from "../seo";
import { SeoLayout } from "../seo-layout";
export const metadata = buildMetadata({ title: "Share Your MECCA Workplace Experience | Inside MECCA", description: "Current and former MECCA employees can share workplace experiences, documentary evidence or interview interest with clear consent choices.", path: "/share-story" });
export default SeoLayout;

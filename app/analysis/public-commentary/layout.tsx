import { buildMetadata } from "../../seo";
import { SeoLayout } from "../../seo-layout";

export const metadata = buildMetadata({
  title: "Public Commentary About Working at MECCA",
  description: "Read de-identified excerpts from publicly posted MECCA workplace comments, clearly labelled as unverified public commentary rather than findings.",
  path: "/analysis/public-commentary",
});

export default SeoLayout;

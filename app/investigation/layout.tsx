import { buildMetadata } from "../seo";
import { SeoLayout } from "../seo-layout";
export const metadata = buildMetadata({ title: "MECCA Workplace Investigation Framework | Inside MECCA", description: "Understand the nine research modules, questions, evidence boundaries and outputs guiding the independent Inside MECCA workplace investigation.", path: "/investigation" });
export default SeoLayout;

import { buildMetadata } from "../../seo";
import { SeoLayout } from "../../seo-layout";
export const metadata = buildMetadata({ title: "MECCA Corporate History | Inside MECCA", description: "A source-led MECCA corporate timeline that separates verified records, working research and unresolved historical questions.", path: "/research-centre/corporate-history" });
export default SeoLayout;

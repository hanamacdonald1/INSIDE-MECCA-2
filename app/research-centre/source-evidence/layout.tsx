import { buildMetadata } from "../../seo";
import { SeoLayout } from "../../seo-layout";
export const metadata = buildMetadata({ title: "MECCA Public Source Evidence | Inside MECCA", description: "Open official public records and attributed reporting through direct source links, with source status and evidentiary limits stated.", path: "/research-centre/source-evidence" });
export default SeoLayout;

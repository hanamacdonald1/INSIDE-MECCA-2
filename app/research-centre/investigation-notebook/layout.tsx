import { buildMetadata } from "../../seo";
import { SeoLayout } from "../../seo-layout";
export const metadata = buildMetadata({ title: "Investigation Notebook | Inside MECCA", description: "A transparent log of Inside MECCA research activity, evidence status, open questions and what has not yet been established.", path: "/research-centre/investigation-notebook" });
export default SeoLayout;

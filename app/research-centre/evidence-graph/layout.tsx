import { buildMetadata } from "../../seo";
import { SeoLayout } from "../../seo-layout";
export const metadata = buildMetadata({ title: "MECCA Evidence Graph | Inside MECCA", description: "Explore how source types connect to MECCA workplace research questions, with relevance clearly separated from proof, causation and findings.", path: "/research-centre/evidence-graph" });
export default SeoLayout;

import { buildMetadata } from "../../seo";
import { SeoLayout } from "../../seo-layout";
export const metadata = buildMetadata({ title: "MECCA Public Claims Database | Inside MECCA", description: "An attributed register of MECCA public statements, related sources, evidence limits and the questions needed to assess each claim.", path: "/research-centre/public-claims" });
export default SeoLayout;

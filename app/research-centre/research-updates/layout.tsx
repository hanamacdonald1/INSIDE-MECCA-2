import { buildMetadata } from "../../seo";
import { SeoLayout } from "../../seo-layout";
export const metadata = buildMetadata({ title: "Research Updates | Inside MECCA", description: "Track dated changes to the Inside MECCA evidence archive, editorial method, source status, research records and substantive corrections log.", path: "/research-centre/research-updates" });
export default SeoLayout;

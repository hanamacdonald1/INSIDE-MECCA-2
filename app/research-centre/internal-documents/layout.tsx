import { buildMetadata } from "../../seo";
import { SeoLayout } from "../../seo-layout";
export const metadata = buildMetadata({ title: "MECCA Restricted-Source Register | Inside MECCA", description: "Browse high-level editorial status summaries for supplied workplace material. No internal source file, image, quotation, extract or download is published.", path: "/research-centre/internal-documents" });
export default SeoLayout;

import { buildMetadata } from "../seo";
import { SeoLayout } from "../seo-layout";
export const metadata = buildMetadata({ title: "MECCA Workplace Research Centre | Inside MECCA", description: "Explore the source-labelled evidence archive behind the independent MECCA workplace investigation, including records, gaps and research status.", path: "/research-centre" });
export default SeoLayout;

import { buildMetadata } from "../seo";
import { SeoLayout } from "../seo-layout";
export const metadata = buildMetadata({ title: "Workplace Change Agenda | Inside MECCA", description: "See how supported findings could become practical workplace recommendations, accountable responses and independently verifiable change.", path: "/change-agenda" });
export default SeoLayout;

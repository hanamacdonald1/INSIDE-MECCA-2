import { buildMetadata } from "../seo";
import { SeoLayout } from "../seo-layout";
export const metadata = buildMetadata({ title: "Work With Inside MECCA | Investigation Roles", description: "Contribute an account, offer relevant expertise, collaborate carefully or share the independent Inside MECCA workplace investigation.", path: "/join-movement" });
export default SeoLayout;

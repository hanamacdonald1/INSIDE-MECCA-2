import { buildMetadata } from "../../seo";
import { SeoLayout } from "../../seo-layout";
export const metadata = buildMetadata({ title: "Source Document Access | Inside MECCA", description: "How Inside MECCA decides whether internal workplace documents can be published, restricted or linked with provenance and privacy safeguards.", path: "/employer-commitments/source-access" });
export default SeoLayout;

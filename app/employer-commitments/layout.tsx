import { buildMetadata } from "../seo";
import { SeoLayout } from "../seo-layout";
export const metadata = buildMetadata({ title: "MECCA Employer Commitments Research | Inside MECCA", description: "Browse independent, paraphrased research reconstructions and public workplace statements, with no internal source-file access and clear evidence limits.", path: "/employer-commitments" });
export default SeoLayout;

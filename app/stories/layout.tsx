import { buildMetadata } from "../seo";
import { SeoLayout } from "../seo-layout";
export const metadata = buildMetadata({ title: "MECCA Employee Experiences & Workplace Stories", description: "Read carefully framed MECCA employee experiences and workplace case studies, published with consent, evidence labels, context and clear limitations.", path: "/stories" });
export default SeoLayout;

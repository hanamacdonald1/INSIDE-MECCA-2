import { buildMetadata } from "../seo";
import { SeoLayout } from "../seo-layout";
export const metadata = buildMetadata({ title: "MECCA Regulatory Actions & Public Record", description: "Review attributed regulatory action, company acknowledgements, reported allegations, litigation and open questions involving MECCA Brands, with evidence limits.", path: "/public-record" });
export default SeoLayout;
